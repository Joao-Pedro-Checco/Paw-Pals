import {Text} from 'react-native';
import {Container} from "../../styles/feed/NewPostStyles";

const NewPostScreen = ({navigation}) => {
    return (
        <Container>
            <Text>New Post Screen</Text>
        </Container>
    );
};

export default NewPostScreen;
